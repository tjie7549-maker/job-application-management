---
name: offer
description: 管理中文求职投递进度：从招聘页面、邮件、截图或用户说明提取并维护投递、筛选、测评、面试、Offer 等状态；适用于“/offer”或要求更新投递表、汇总求职进度时。
---

# /offer：求职进度管理

将零散招聘信息维护为可复盘的本地投递表。真实记录只写入用户指定的追踪文件（默认工作区的 `application-tracker.html`），绝不写入本 skill、README 或公开资源。

## 记录与隐私

- 从用户说明、招聘页面、截图或邮件中提取公司、岗位、Base、日期、渠道、状态和下一步；不凭空补全缺失信息。
- 不记录密码、验证码、MFA、身份证件、申请编号，或带有 `userId`、申请编号等个人追踪参数的链接。保留招聘入口时，优先使用去除这些参数后的通用链接。
- 需要登录、验证码、CAPTCHA 或最终提交时，由用户完成；协助填写前先使用用户已确认的真实信息，提交前展示关键内容并取得确认。
- 浏览器操作优先使用 Microsoft Edge；是否启动受控自动化、关闭窗口或暴露既有浏览器会话，须取得用户明确授权。

## 状态判定

只按证据更新状态，后续阶段覆盖同一公司同一岗位的早期状态。

| 证据 | 状态 |
| --- | --- |
| 投递成功、投递完成 | 已投递 |
| 简历初筛、发送 HR、简历筛选中 | 筛选中 |
| 用户明确说明进入笔试或测评，或截图明确显示“测评环节” | 测评中（笔试/测评） |
| 明确面试邀请或面试安排 | 一面 / 二面 / 三面 / 四面 / HR面 |
| 明确 Offer | Offer |
| 明确拒信或流程结束 | 已结束 |

普通自动回执不能推断为面试或 Offer；证据不足时保留原状态，或标记“待确认”并说明缺口。

- 面试轮次不明确时使用 `面试（待细分）`；获得轮次信息后再更新。状态汇总中的“面试”应汇总以上所有面试轮次。
- 仅显示“流程进行中”不能推断为筛选或面试，保持 `已投递`；截图中的“站点/面试站点”也不能覆盖用户明确提供的岗位 Base。

## 表格规范

- 同一公司同一岗位合并为一条；同品牌但法定主体无法确认相同时不要强行合并。
- 公司和岗位展示为便于扫读的关键词，而不是冗长法定全称或完整职位标题。
- “企业性质”默认填 `私企`，仅在用户明确说明时改为其他性质。
- Base 未提供则写 `—` 或留空；不得根据总部、岗位名称或截图外信息猜测。
- 末列保留“想去程度”下拉框，选项固定为 `海投`、`有点意思`、`想去`，默认不选且由用户手动选择。页面可用本地持久化保存其选择。
- 顶部状态卡至少含投递总数及各状态数量；投递总数必须等于表格有效记录数。
- 表格主体与顶部状态卡使用同一内容宽度并左右对齐，优先保证窄屏可横向查看。
- 对 HTML 追踪表，顶部应提供仅按公司名称筛选的搜索框；筛选不得改变状态卡统计、想去程度排序或流程排序。
- 每条记录应保留手动“修改”和“删除”入口；修改至少覆盖公司、岗位、Base、日期（可保留时分）、企业性质、渠道、状态、链接和下一步，并在保存后刷新排序与状态卡。想去程度继续由末列下拉独立保存。
- HTML 追踪表的手动新增、修改、删除以及状态/想去程度选择必须通过浏览器本地存储持久化；重新打开同一页面时应还原这些变更，并与 HTML 中后来新增的静态记录合并，不能因旧快照遗漏新投递。已删除记录应继续保持删除状态。
- 打开“新增”或“修改”弹窗前记录页面滚动位置；取消后恢复该位置。新增、弹窗修改、状态下拉修改或想去程度下拉修改后，都应在重排完成后定位到刚操作的记录并使其位于屏幕中间，并用短暂高亮提示该行，避免跳到顶部或底部。
- 未提供渠道则留空；只保存实际招聘入口。不要将聊天应用、登录跳转或其他非招聘页面作为招聘链接。
- HTML 追踪表中的招聘入口链接应在新标签页打开，并设置 `rel="noopener noreferrer"`；静态记录和手动新增、修改生成的链接都必须保持这一行为。

## 更新与交付

1. 先读取现有追踪表，保留用户已有的想去程度、状态选择、搜索和无关样式。
2. 合并新增或更新的记录，重新计算状态卡；避免改动无关条目。截图给出投递时分时应原样保留。
3. 检查总数、状态数量、链接脱敏、面试轮次、下拉默认值，以及搜索、修改和删除交互不受影响。
4. 交付时简要说明新增/变化记录、总数及待用户处理的下一步；提供追踪表的绝对路径。
